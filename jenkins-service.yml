apiVersion: v1
kind: Service
metadata:
  name: jenkins-service
  labels:
    name: jenkins-service
    app: jenkins-deployment
spec:
  type: loadBalancer
  ports:
    port: 8080
    targetPorts: 8080
  selector:
    name: jenkins
    app: jenkins-deployment
    
